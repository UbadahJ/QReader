<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/add"
        android:icon="@drawable/external_link"
        android:title="Add book from link"
        app:iconTint="?android:attr/textColorPrimary"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/search"
        android:icon="@drawable/search"
        android:title="@string/search"
        app:iconTint="?android:attr/textColorPrimary"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu"
        android:icon="@drawable/menu_dots"
        android:title=""
        app:iconTint="?android:attr/textColorPrimary"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/menu_refresh_books"
                android:icon="@drawable/refresh"
                android:title="Refresh"
                app:iconTint="?android:attr/textColorPrimary" />
            <item
                android:id="@+id/menu_generate_index"
                android:icon="@drawable/download"
                android:title="Download Books Index"
                app:iconTint="?android:attr/textColorPrimary" />
            <item
                android:id="@+id/menu_generate_index_force"
                android:icon="@drawable/download"
                android:title="Download Books Index (Force)"
                app:iconTint="?android:attr/textColorPrimary" />
        </menu>
    </item>
</menu>